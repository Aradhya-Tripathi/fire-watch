version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.8
    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            python3 -m venv rain
            . rain/bin/activate
            pip install -r requirements.txt
      - run:
          name: run tests
          command: |
            . rain/bin/activate
            bash run_server_tests.sh 0